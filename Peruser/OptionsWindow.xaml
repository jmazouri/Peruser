<Controls:MetroWindow x:Class="Peruser.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="Settings" Height="300" Width="300" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <xctk:PropertyGrid Grid.Row="0" SelectedObject="{Binding Config}"></xctk:PropertyGrid>
        <Button Grid.Row="1" Height="32" Width="64" Click="Button_Click">OK</Button>
    </Grid>
</Controls:MetroWindow>
